*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.fleet
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
Homestead.json
Homestead.yaml
Thumbs.db

# Live Sepio testing-environment data. The operator drops this file into
# the project root before running LiveSepioSeeder and removes it after —
# it contains real Sepio JWTs and encrypted credentials, so it must not
# be committed.
/database/o2o.sql
/database/o2o.local.sql
